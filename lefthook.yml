skip_lfs: true
output:
  - success
  - failure
pre-commit:
  commands:
    format:
      run: just fmt
      stage_fixed: true
pre-push:
  parallel: true
  commands:
    test:
      run: just test
    lint:
      run: just lint
